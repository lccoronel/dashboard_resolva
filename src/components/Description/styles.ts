import styled from 'styled-components';

export const Container = styled.p`
  font-size: 13px;
  color: var(--textColor);
  font-weight: 400;
  font-family: "Roboto", sans-serif;
  text-align: center;
`;
